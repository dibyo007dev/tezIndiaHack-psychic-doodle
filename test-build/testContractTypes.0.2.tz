Storage: sp.TRecord(admin = sp.TAddress, name = sp.TString, ownerToBalance = sp.TBigMap(sp.TAddress, sp.TInt), symbol = sp.TString, tokenApprovals = sp.TBigMap(sp.TString, sp.TAddress), tokenHolderToID = sp.TBigMap(sp.TAddress, sp.TSet(sp.TString)), tokenIdToOwner = sp.TBigMap(sp.TString, sp.TAddress), tokensMinted = sp.TSet(sp.TString))
Params: sp.TVariant(approve = sp.TRecord(approve_to = sp.TAddress, token_id = sp.TString), burnDataToken = sp.TRecord(token_id = sp.TString), mintDataToken = sp.TRecord(address = sp.TAddress, token_id = sp.TString), transfer = sp.TRecord(to = sp.TAddress, token_id = sp.TString), transferFrom = sp.TRecord(f = sp.TAddress, t = sp.TAddress, token_id = sp.TString))